when:
  - event: [push, pull_request]
steps:
  verify:
    image: node:22-alpine
    commands:
      - npm ci
      - npm run fmt:check
      - npm run typecheck
      - npm run test:unit
      - npm run smoke
